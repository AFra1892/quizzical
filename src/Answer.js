import React from "react";



export default function Answer(props){
    
    <button 
        onClick={props.handle}
        // style={{ backgroundColor: props.colored ? "rgba(7, 7, 56, 0.56)" :" white"}}
    >
        {props.val}
    
    </button>
}